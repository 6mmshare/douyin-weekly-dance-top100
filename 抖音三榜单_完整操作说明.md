# 抖音三榜单最终精简操作说明

根目录：

```text
D:\douyin-weekly-dance-top100-main
```

## 1. 覆盖方法

**不要删除现有项目目录。**

将本压缩包里的文件直接复制到：

```text
D:\douyin-weekly-dance-top100-main
```

选择“替换目标中的文件”。

本包故意不包含：

```text
.venv
data
logs
output
debug_screenshots
creators_watchlist.xlsx
任何登录 Profile / Cookie
任何 checkpoint
任何现有网页 / Excel / JSON / CSV 结果
```

因此不会替换你已经抓好的数据和网站正在显示的数据。

---

# 2. 以后只需要看根目录 00～10

```text
00_打开完整操作说明.cmd
01_检查抖音登录.cmd

02_小姐姐榜_运行采集Top100.cmd
03_小姐姐榜_用现有数据生成Top100.cmd
04_小姐姐榜_打开最新结果.cmd

05_精选博主榜_抓取最近7天.cmd
06_精选博主榜_生成Top100.cmd
07_精选博主榜_打开最新结果.cmd

08_宠物榜_运行采集Top100.cmd
09_宠物榜_用现有数据生成Top100.cmd
10_宠物榜_打开最新结果.cmd
```

这批快捷指令现在**直接调用 Python 核心文件**，不再依赖旧的：

```text
run_visible.bat
run_weekly.bat
OPEN_LATEST_RESULT.bat
SHOW_PROGRESS.bat
```

---

# 3. 三个榜单每周怎么跑

## 小姐姐榜

正常完整采集：

```text
02_小姐姐榜_运行采集Top100.cmd
→ 04_小姐姐榜_打开最新结果.cmd
→ 网页人工筛选最终Top50
→ 导出人工筛选Excel
```

已有候选，只想重新生成网页：

```text
03_小姐姐榜_用现有数据生成Top100.cmd
→ 自动打开最新结果
```

## 精选博主榜

```text
05_精选博主榜_抓取最近7天.cmd
→ 06_精选博主榜_生成Top100.cmd
→ 自动打开精选榜最新结果
→ 人工筛到50条
→ 导出最终Top50 Excel
```

## 宠物榜

正常：

```text
08_宠物榜_运行采集Top100.cmd
→ 09_宠物榜_用现有数据生成Top100.cmd
→ 自动打开宠物榜最新结果
→ 人工筛选Top50
→ 导出人工筛选Excel
```

如果已经有很多候选，或者搜索被限流：

```text
停止搜索
→ 不清 checkpoint
→ 09_宠物榜_用现有数据生成Top100.cmd
→ 自动打开最新结果
```

---

# 4. 互动数据

三个榜单都会尽量获取并保存：

```text
播放
点赞
评论
分享
收藏
```

结果网页和人工筛选 Excel 中也会保留这些字段。

如果抖音公开页面或接口某次没有返回某一项，程序保留 `0`，不会伪造数字。

---

# 5. 最近7天逻辑

搜索卡片如果只有：

```text
昨天
前天
1天前
2天前
...
6天前
```

程序的“用现有数据生成Top100”功能可以利用这些相对时间做最近7天兜底判断。

以下模糊时间不会强行当作最近7天：

```text
1周前
2周前
1月前
```

正常实时搜索、验证码、断点、详情补全等成熟采集流程保持原逻辑，不额外改动。

---

# 6. 覆盖完成后，可以删除的根目录旧文件

确认 `00～10` 能正常打开/运行后，下面这些旧入口已经不再被新快捷指令依赖，可以从根目录删除：

```text
apply_strict_week_window_fix.py
install.bat
login_once.bat
OPEN_LATEST_RESULT.bat
RESET_CURRENT_PROGRESS.bat
run_demo.bat
run_visible.bat
run_weekly.bat
SHOW_PROGRESS.bat
```

如果只是自己使用、又不在意 GitHub 文档，还可以自行决定是否保留：

```text
README.md
SECURITY.md
LICENSE
requirements.txt
```

但推荐继续保留这四个，因为它们是开源仓库的标准说明文件。

**不要删除：**

```text
config.yaml
weekly_dance_ranker.py
.venv
data
logs
output
debug_screenshots
douyin_creator_recent7d_tool
douyin_pet_dance_top100
```

---

# 7. 数据安全

更新原则：

```text
只覆盖代码
不覆盖 data
不覆盖 output
不覆盖登录环境
不清 checkpoint
```

遇到异常、限流或停电时，优先保住：

```text
data\checkpoints
data\profiles
output
```

不要先执行清除断点。

---

# 8. 最终建议保留的根目录结构

```text
D:\douyin-weekly-dance-top100-main\
├─ .venv\
├─ data\
├─ debug_screenshots\
├─ logs\
├─ output\
├─ douyin_creator_recent7d_tool\
├─ douyin_pet_dance_top100\
│
├─ 00_打开完整操作说明.cmd
├─ 01_检查抖音登录.cmd
├─ 02_小姐姐榜_运行采集Top100.cmd
├─ 03_小姐姐榜_用现有数据生成Top100.cmd
├─ 04_小姐姐榜_打开最新结果.cmd
├─ 05_精选博主榜_抓取最近7天.cmd
├─ 06_精选博主榜_生成Top100.cmd
├─ 07_精选博主榜_打开最新结果.cmd
├─ 08_宠物榜_运行采集Top100.cmd
├─ 09_宠物榜_用现有数据生成Top100.cmd
├─ 10_宠物榜_打开最新结果.cmd
│
├─ weekly_dance_ranker.py
├─ config.yaml
├─ 抖音三榜单_完整操作说明.md
├─ README.md
├─ requirements.txt
├─ LICENSE
└─ SECURITY.md
```

这样以后日常操作只看 `00～10` 即可。
